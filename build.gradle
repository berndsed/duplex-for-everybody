plugins {
    id 'groovy'
}

group 'de.kieseltaucher.duplex.scan.for.everybody'
version '1.0-SNAPSHOT'

task asGroovyScript(type: Zip) {
    description 'Creates a distribution containing a groovy script'
    group 'distribution'

    from layout.projectDirectory.file('src/main/groovy')
    from layout.projectDirectory.file('distributions/groovy-script/simplex2duplex.groovy')
    archiveName = "${project.name}-groovy-script-${project.version}.zip"
}

task asAwsLambda(type: Zip) {
    description 'Creates an aws lambda deployment package'
    group 'distribution'

    from compileGroovy
    into('lib') {
        from configurations.runtimeClasspath
    }
    archiveName = "${project.name}-aws-lambda-${project.version}.zip"
}

repositories {
    mavenCentral()
}

dependencies {
    compile 'org.codehaus.groovy:groovy:2.5.8'
    compile 'org.codehaus.groovy:groovy-json:2.5.8'
    compile 'org.apache.pdfbox:pdfbox:2.0.24'
    compile 'com.amazonaws:aws-lambda-java-core:1.2.1'
    testCompile 'org.spockframework:spock-core:1.3-groovy-2.5'
}
